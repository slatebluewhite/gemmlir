# Build trees
build/
build-*/
.cache/
compile_commands.json

# Objects and LLVM IR, except the reference dumps docs/ walks through
*.o
*.ll
!docs/reference/**/*.ll

# Python
__pycache__/
*.pyc

# What regression/export/export_all.sh regenerates
regression/models/

# What examples/mlperf-tiny/export.py regenerates
examples/mlperf-tiny/*_cal.mlir
examples/mlperf-tiny/*_raw.mlir
examples/mlperf-tiny/*_data.h

# What demo/run.sh regenerates: about 11 MB a model of image bytes and
# reference logits, and the MLIR they were calibrated with.
demo/*_demo_data.h
demo/*_demo_cal.mlir
demo/*_demo_raw.mlir

# Local paths: `make` reads this if it exists (see the head of the Makefile).
config.mk
